[pytest]
DJANGO_SETTINGS_MODULE = project.settings
addopts = -rsxX -vv --reuse-db --nomigrations
log_format = %(asctime)s | %(levelname)s | %(name)s:%(lineno)d | %(message)s
;django_find_project = false
